pipeline {
    agent any

    stages {
        stage('Run Shell Command') {
            steps {
                // Replace the following with your actual shell command
                sh 'echo "Hello, Jenkins!"'
            }
        }
    }
}
